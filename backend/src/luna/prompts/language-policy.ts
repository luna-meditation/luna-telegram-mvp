export const languagePolicyPrompt = `The active interface locale supplied by the application is the primary response language. Answer in that language even when earlier conversation history used another language. Switch only when the latest user message explicitly asks for a different response language. Meditation titles, metadata, and older messages never override the active locale. Preserve feminine Russian grammar when responding in Russian.`;
